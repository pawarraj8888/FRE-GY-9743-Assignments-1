# FRE-GY-9743-Assignments-2026fall
Assignments for NYU, FRE-GY 9743, Fall 2026.

---

## Assignment 1 submission: Raj Pawar (`Raj_Pawar_Branch`)

**Project page:** https://pawarraj8888.github.io/FRE-GY-9743-Assignments-1/

| Deliverable | Location |
|---|---|
| PCLC interpolator (`interpolate`, `integrate`, `gradient_wrt_ordinate`, `gradient_of_integrated_value_wrt_ordinate`) | `fixedincomelib/utilities/numerics.py`, class `Interpolator1DPCP` |
| Bump-and-reval validation of both gradients | `hw1_interpolator.ipynb` (`bump_reval_interpolator_integrand` filled in; all checks executed, every `diff ≈ 0`) and `tests/test_interpolator_pcp.py` (20 tests) |
| Bond forward business, Q1–Q5 and \*Q5 | Markdown section **Part I** at the start of `hw1_interpolator.ipynb`, plus a numerical cash-and-carry illustration after the template cells |
| Typeset write-up of Q1–Q5 and the interpolator | `writeup/Assignment1_Writeup_RajPawar.pdf` (source: `.tex` in the same folder) |
| Project page (interactive interpolator, code walk-through, validation tables, Part I summary) | https://pawarraj8888.github.io/FRE-GY-9743-Assignments-1/ (source: `docs/index.html`, regenerated by `docs/build_dashboard.py`) |

### How to run

```bash
pip install -r requirements.txt
jupyter notebook hw1_interpolator.ipynb      # Run All: every check prints "diff ≈ 0"
python tests/test_interpolator_pcp.py         # or, with pytest installed: python -m pytest tests -q
```

Inside the professor's code the changes are confined to `Interpolator1DPCP` (the four `## TODO` methods, input
validation and two private methods), one small scalar validator next to it, and the notebook's `## TODO`; the class
hierarchy (`Interpolator1D` → `Interpolator1DPCP`), `InterpolatorFactory` and the `qf*` APIs are unchanged. The
notebook additionally carries the Part I answers, a numerical example and the test runner.
